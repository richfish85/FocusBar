<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="FocusBar.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FocusBar"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="FocusBar">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="CurrentTaskText" Grid.Row="0" Text="Enter task here..."
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               Margin="10"
               FontSize="14"
               TextWrapping="Wrap"/>

        <ProgressBar x:Name="TimerProgressBar"
                 Grid.Row="1"
                 HorizontalAlignment="Stretch"
                 VerticalAlignment="Stretch"
                 Maximum="100"
                 Value="100"
                 Margin="20,0"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button x:Name="StartPauseButton" Content="Start" Margin="5" Click="StartPauseButton_Click" />
            <Button x:Name="SkipButton" Content="Skip" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
